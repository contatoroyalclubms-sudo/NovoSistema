FROM node:18-alpine
WORKDIR /app

# Copiar apenas arquivos do frontend
COPY paineluniversal/frontend/package*.json ./
RUN npm ci --only=production

COPY paineluniversal/frontend/ .
RUN npm run build

# Servir aplicação
RUN npm install -g serve
EXPOSE 3000
CMD ["serve", "-s", "dist", "-l", "3000"]